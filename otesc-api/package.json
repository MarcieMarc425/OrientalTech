{
	"name": "otesc-api",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node ./dist/index.js",
		"heroku-postbuild": "cd client && npm install && npm run build",
		"local-start": "nodemon --exec babel-node -- src/index.js",
		"local-build": "./node_modules/.bin/babel  ./src --experimental --source-maps-inline -d ./dist"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"body-parser": "^1.18.3",
		"dotenv": "^6.2.0",
		"express": "^4.16.4",
		"mongoose": "^5.4.1"
	},
	"devDependencies": {
		"nodemon": "^1.18.9",
		"babel-cli": "^6.26.0",
		"babel-preset-env": "^1.7.0"
	}
}
